import { Typography, Paper } from '@mui/material';

export default function Settings() {
  return (
    <Paper elevation={3} className="p-8">
      <Typography variant="h4" className="mb-6">Settings</Typography>
      <Typography>Settings page</Typography>
    </Paper>
  );
}
